import { AppPlaceholder } from "@/components/app-placeholder";

export default function SecurityPage() {
  return (
    <AppPlaceholder
      title="SEGURIDAD"
      description="Gestiona contrasena y metodos de acceso de tu cuenta."
    />
  );
}
